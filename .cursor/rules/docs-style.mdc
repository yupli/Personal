---
description: 文档格式规范；加粗以「仅冒号前」为唯一例外
globs: "**/*.md"
alwaysApply: false
---

# 文档格式规范

## 加粗（统一规则，唯一默认）

- **可（且仅可）用 `**` 加粗**：仅当在正文里使用「**全角`：` 或 半角`:`** 前的**短标签/栏目名**」时，**仅**对该**紧挨在冒号之前** 的那一段文字加粗，形成如 **适用**：、**核心**：、**对象**：、**特点**：、**注**：、**含义**： 等，冒号**之后** 一律**不加粗**。
- **除上述结构外，全文均不使用加粗 `**`。** 包括但不限于：为强调、术语、对比、**英文**、**算法名**、**表格** 单元格、**算法框内**的 `###` 步骤名、**列表** 里除「标签：」体例以外的任何词。若需分层，用 `##` / `###` 与列表，勿用加粗当标题或反事实强调。

**正确示例**：

```markdown
**适用**：离散结构上的近似求解。

**注**：与替换规则对齐，具体见实现说明。
```

**不要写**（冒号后加粗、或行内无冒号而加粗）：

```markdown
在组合优化中，**模拟退火** 常用于……

- **选择**：……（此项冒号前「选择」可加粗是允许的）
- **锦标赛** 在子样本里取优（「锦标赛」后无本规则下的冒号，**不可** 加粗成「**锦标赛**」作强调）
```

**关于「标签 + 全角括注 + 冒号」**：如 **代表谱系**（本库会逐步展开，此处仅举例）：——仅将**最短的标签词** 置于加粗中、且**在左括号前结束加粗**；括注与冒号**不在** 加粗内（与上条「只冒号前且仅为栏目名」一致，避免整段加粗）。

---

## 标题与 `###` 后换行

- `##` 二级标题后可以空行以分隔大段。
- `###` 三级及以下小标题的**下一行** 必须**直接**接内容/列表/表格，**不要** 空行。

### 正确

```markdown
### 小标题
直接接内容，不空行。
```

### 避免

```markdown
### 小标题

再写内容（中间不空行更好以外的空行不在这里示范）
```

## 算法流程（`::: algorithm`）

- 凡**算法主流程/步骤** 的内容，**必须** 用专属容器包裹。  
- 使用 `::: algorithm 算法名称` 开头，容器内用 `###` 与编号步骤，以 `:::` 结束。  
- 步骤行用 `### 1. 名称` 即可，**不要** 对步骤名用 `**` 加粗（本规则下无冒号则不加粗）。  
- **公式与构建**：VitePress 在部分页面上行内 `$...$`、`\(...\)`  可能预渲染为空白；**算法框** 与易出问题段落**优先** 用 **Unicode/直写** 写符号（如 T₀、Δ、←、exp(·)、s⋆），再视整站 MathJax 修复情况考虑 LaTeX。

## 可折叠块（`details` / `summary`）

- 长并列说明可用 HTML `<details><summary>…</summary>…</details>`。  
- `summary` 行若无「**标签** + 冒号 + 后文」体例，则 **summary 内整体不加粗**；若用「一、**标题**」等无冒号，则**一、** 等编号也不加粗，保持普通字。  
- `summary` 与内层 Markdown 之间空一行。默认折叠；需默认展开时设 `<details open>`。

## 其他

- 新增或润色 `docs/**/*.md` 时，**扫一遍** `**`，**凡不符合「仅冒号前标签加粗」的，一律删除** `**`；表格、索引表尽量**全表无加粗**（除你刻意写的「**列名**：（若表内采用标签：体例可保留」——若表内不用标签：体例，则**零加粗**更佳）。

---

（若要引用范本页，以结构清晰、**加粗面积极少** 的 `docs/or-opt/branch-price-and-cut.md` 为对照；历史文档若有多处加粗，按本规则**逐步收敛**即可。）
