---
description: 文档格式规范；加粗以「仅冒号前」为唯一例外
globs: "**/*.md"
alwaysApply: false
---

# 文档格式规范

## 加粗（统一规则，唯一默认）

- 可（且仅可）用 Markdown 的 `**` 加粗：仅当在正文里出现「短标签 + 全角冒号 `：` 或半角冒号 `:`」时，仅对紧挨在冒号之前的那段标签文字加粗，形成如 `**适用**：`、`**核心**：`、`**对象**：`、`**特点**：`、`**注**：`、`**含义**：` 等；冒号之后一律不加粗。
- 除上述结构外，全文均不使用 `**`。包括但不限于：为强调、术语、对比、英文专名、算法名、表格单元格、算法框内的 `###` 步骤名、列表里除「标签 + 冒号」体例以外的词。若需分层，用 `##` / `###` 与列表，勿用加粗当标题或强调。

正确示例：

```markdown
**适用**：离散结构上的近似求解。

**注**：与替换规则对齐，具体见实现说明。
```

不要写（冒号后加粗、或行内无冒号而加粗）：

```markdown
在组合优化中，**模拟退火** 常用于……

- **选择**：……（冒号前「选择」可加粗）
- **锦标赛** 在子样本里取优（「锦标赛」后无本规则下的冒号，不可加粗）
```

关于「标签 + 全角括注 + 冒号」：如 `**代表谱系**（本库会逐步展开，此处仅举例）：`——仅将最短标签词置于加粗中、且在左括号前结束加粗；括注与冒号不在加粗内。

---

## 标题与 `###` 后换行

- `##` 二级标题后可以空行以分隔大段。
- `###` 三级及以下小标题的下一行必须直接接内容/列表/表格，不要空行。

### 正确

```markdown
### 小标题
直接接内容，不空行。
```

### 避免

```markdown
### 小标题

再写内容（勿在标题后空行再接正文）
```

## 算法流程（`::: algorithm`）

- 凡算法主流程/步骤的内容，必须用专属容器包裹。  
- 使用 `::: algorithm 算法名称` 开头，容器内用 `###` 与编号步骤，以 `:::` 结束。  
- 步骤行用 `### 1. 名称` 即可，不要对步骤名使用 `**` 加粗（无「标签+冒号」则不加粗）。  
- **数学 / LaTeX / MathJax 定界、式号、CommonMark 与 `\[ \]` `\(` `\)` 的坑、行内/块级写法、算法框内公式**：一律按 **`.cursor/rules/docs-math-mathjax.mdc`** 执行；**不要**在正文里用 **注** 向读者解释「本文件单独编号」等——该约定只写在 `docs-math-mathjax` 中。

## 附图：何时用图、文字与公式一律自撰

- **文字叙述与数学公式**（含教材中的定义、模型、式组）**必须**在正文中以 Markdown 与 `.cursor/rules/docs-math-mathjax.mdc` 规定的 `$` / `$$` 自行撰写与排版，**不得**用教材书页、讲义截屏**整页**代替可重排的正文或公式。式号、与纸书互查的说明可用简短 **注** 或行内补一句原书式号，勿依赖「再贴一张有字的图」来承载同一段内容。  
- **允许**使用图片的情形：仅当内容**无法**或**极难**用纯文字与 LaTeX 在网页上合理还原时，例如**网络/地图类示意图、真实照片、书内特有的复杂线框与标注**等。此类图可放在 `docs/{知识目录}/resources/`（如 `fig-*.png`），用 `<figure>` + `<img>` 引用，并加简短、**不含需 MathJax 渲染的公式** 的 `figcaption`（`figcaption` 内勿写 `$`；见 `docs-math-mathjax`）。  
- 若用户随对话**附上**截图但正文已能完整自撰，**不要**为存档而再嵌一遍重复图。  
- **不要**在正文用 **注** 重复 `docs-math-mathjax` 里已写的式号全库规则本身。

## 可折叠块（`details` / `summary`）

- 长并列说明可用 HTML `<details><summary>…</summary>…</details>`。  
- `summary` 行一般不使用加粗（该处常无「标签+冒号」体例）；编号如「一、」与标题均用普通字。  
- `summary` 与内层 Markdown 之间空一行。默认折叠；需默认展开时设 `<details open>`。

## 其他

- 新增或润色 `docs/**/*.md` 时，扫一遍 `**`，凡不符合「仅冒号前标签加粗」的，一律删除；表格、索引表以零加粗为佳。
- 运筹 `docs/or-opt/` 下**元启发式算法子页**的开篇结构、主循环 `algorithm` 与内层子过程顺序等，见项目规则 `docs-or-opt-heuristics.mdc`；**不要**在用户文档中重复粘贴该体例全文。

范本：结构清晰、加粗极少的 `docs/or-opt/exact/branch-price-and-cut.md`；历史文档可按本规则逐步收敛。
