---
description: 文档格式规范要求
globs: "**/*.md"
alwaysApply: false
---

# 文档格式规范

## 标题与内容格式

**小标题（###）下面直接跟内容，不要空行。**

### ✅ 正确写法

```markdown
### 小标题
直接跟内容，不空行。
```

### ❌ 错误写法

```markdown
### 小标题

不要这样空行后再写内容。
```

## 算法流程样式

**凡是涉及算法流程的内容，必须使用专属样式框。**

### ✅ 正确写法

```markdown
::: algorithm 算法名称

### 1. 步骤一
内容描述...

### 2. 步骤二
内容描述...

:::
```

### ❌ 错误写法

```markdown
## 算法流程

### 1. 步骤一
直接写步骤，不用容器包裹。
```

### 规则说明
- 使用 `::: algorithm 算法名称` 作为容器开头
- 容器内用 `###` 编号步骤（1. 2. 3. ...）
- 以 `:::` 结束容器
- 容器会显示为带青色边框和 ▶ 图标的专属框

## 可折叠块（`details` / `summary`）

用于**并列、同类多段**且希望**默认收拢**的说明（如分类下各子条的长列表），在 Markdown 中可直接写 HTML：

```html
<details>
<summary>一、<strong>基础启发式</strong> (Simple Heuristics)</summary>

- **对象**：……
- **特点**：……

</details>
```

- **`summary` 行**：编号（如 一、）为普通字；**仅**分类名/标签词用 `**` 加粗，半角括号的英文名 **不加粗**；与上节「只加粗括号前词」一致。需要默认展开时加 `open` 属性：`<details open>`（少用，以折叠为默认较省屏）。
- **`summary` 与正文之间**：空一行，便于 VitePress 将正文解析为 Markdown。
- 仍遵守上文「**对象**、**特点**、列表」等加粗规则，勿在可折叠内放宽。

## 其他格式规则

- `##` 二级标题后可以空行（用于分隔大章节）
- `###` 三级及以下小标题后**直接跟内容**，不空行
- 列表、表格等元素紧跟小标题，不空行

## 加粗范式（全站正文的默认做法）

以 `docs/or-opt/branch-price-and-cut.md` 为范本：**仅在「结构标签」上加粗**，正文、表格内文、行内一般术语**不要**随手加粗当强调。

**可以加粗的情况（示例，可按文意类推）：**

- 引出结构的提示语：**典型内层循环**、**基本思想**、**核心** 等，后接「可理解为：」或冒号，再接正文。
- 有序步骤的**小标题**（只加粗**名称**）：`1. **列生成**（定价）`（编号在列表中，名称加粗，括号**不加粗**）、`2. **割分离**`、`3. **终止**`；算法框外的步骤可写成 `**(1)**` 行首仅当与原文编号体例一致，否则优先用 `1. **名称**`。
- 段首标签：**关键点**：、**小结**：、**注**：、**说明**：、**效果**： 等，只加粗标签词，冒号后正文不加粗。若标签后带全角或半角括号的补充，**只加粗标签词本身，括号与括号内说明一律不加粗**（如 **定义**（直观）：、**代表谱系**（本库会逐步展开，此处仅举例）：）。
- 行内**小结**（如 **小结**（本节））：只加粗「小结」。

### 带括号的行首标签：正确/错误

**正确**（加粗在冒号/顿号/举例前**停**在标签词末尾）：

```markdown
- **代表谱系**（本库会逐步展开，此处仅举例）：遗传/进化、模拟退火、禁忌搜索、变邻域、粒子群、蚁群、差分进化等。
- **常见分法**（文献中多交叉使用）：选择型（……）、生成型（……）等。
```

**错误**（把括号、括注或整行说明一并加粗，易显乱）：

```markdown
- **代表谱系（本库会逐步展开，此处仅举例）：** 遗传/……
- **选择型**（基于规则、……）**、**生成型**（以语法或……）等（行内类名不要靠加粗当强调，应改为普通字）
```

**不要加粗的情况：**

- 普通强调、同义词、形容词（「非常重要」「紧耦合」等）；
- 段首「对象」「特点」冒号**之后**的整句、举例枚举、**选择型/生成型** 等行内类名，除非就是列表里的**唯一结构标签**；
- 表格单元格里除「表头/第一列条名」外的一般说明（目录索引表尽量全部不加粗，避免花眼）；
- 公式与代码块内的强调。

新增或润色任何 `docs/**/*.md` 时，先扫一遍 `**`，删掉不符合上文的；缺结构标签时，优先用**小节标题**或**冒号段首标签**来分段，少依赖加粗当强调。
