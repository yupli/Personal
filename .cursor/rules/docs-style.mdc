---
description: 文档格式规范要求
globs: "**/*.md"
alwaysApply: false
---

# 文档格式规范

## 标题与内容格式

**小标题（###）下面直接跟内容，不要空行。**

### ✅ 正确写法

```markdown
### 小标题
直接跟内容，不空行。
```

### ❌ 错误写法

```markdown
### 小标题

不要这样空行后再写内容。
```

## 算法流程样式

**凡是涉及算法流程的内容，必须使用专属样式框。**

### ✅ 正确写法

```markdown
::: algorithm 算法名称

### 1. 步骤一
内容描述...

### 2. 步骤二
内容描述...

:::
```

### ❌ 错误写法

```markdown
## 算法流程

### 1. 步骤一
直接写步骤，不用容器包裹。
```

### 规则说明
- 使用 `::: algorithm 算法名称` 作为容器开头
- 容器内用 `###` 编号步骤（1. 2. 3. ...）
- 以 `:::` 结束容器
- 容器会显示为带青色边框和 ▶ 图标的专属框

## 其他格式规则

- `##` 二级标题后可以空行（用于分隔大章节）
- `###` 三级及以下小标题后**直接跟内容**，不空行
- 列表、表格等元素紧跟小标题，不空行

## 密集段落与加粗

- 主题正文为**宋体/思源宋体**风格；用 Markdown 的 `**文字**` 做加粗时，在屏上应为**明显加粗**（字重 700），便于扫读。
- **大段叙述、定义链、因果句**中，对**核心术语**、**结论**、**须记住的判据**使用 `**...**` 标出，避免通篇无强调或通篇全加粗。
- 英文专名、算法缩写（如 **BPC**、**RMP**）在**首次**或**关键处**加粗即可，不必每次出现都加粗。
