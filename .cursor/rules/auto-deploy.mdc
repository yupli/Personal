---
description: 检测到"提交"关键词时自动执行完整部署流程
globs: ""
alwaysApply: true
---

# 自动部署规则

当用户在对话中提到"提交"相关词汇时（如"提交"、"commit"、"push"、"部署"等），自动执行以下完整流程：

## 触发条件
用户消息包含以下关键词：
- 提交
- commit
- push
- 部署
- deploy
- 发布

## 自动执行流程

1. **检查状态**
   - 执行 `git status` 查看是否有未提交的更改

2. **添加更改**
   - 执行 `git add .` 将所有更改添加到暂存区

3. **提交代码**
   - 执行 `git commit -m "<描述>"`，使用合适的提交信息
   - 如果用户没有提供具体描述，询问用户提交信息

4. **推送到远程**
   - 执行 `git push origin main` 推送到 GitHub

5. **部署网站**（如果是 VitePress 项目）
   - 执行 `npm run deploy:gh` 构建并部署到 gh-pages

## 执行前确认

如果更改较多或涉及重要文件，先询问用户：
> "检测到需要提交的更改，是否立即执行提交+推送+部署？"

## 注意事项

- 使用 `block_until_ms: 60000` 或更高等待长时间命令
- 推送失败时检查网络连接，提示用户稍后重试
- 部署成功后提供网站访问链接
- 如果 `.github/workflows/` 中有工作流文件，询问用户使用哪种部署方式（gh-pages 或 GitHub Actions）
