---
description: 任务收尾自动提交、推送与部署；不询问用户是否提交
globs: ""
alwaysApply: true
---

# 自动提交与部署

## 默认行为（必须遵守）

在本仓库中**完成**涉及文件增删改的助理任务后，若 `git status` 显示有**未提交变更**，助理**必须**在收尾阶段**自动**完成以下流程，**不得**询问「要不要提交 / 是否 push / 是否部署」：

1. `git status` 查看变更范围  
2. `git add` 纳入本次任务相关文件（与任务无关的本地修改若存在，优先只暂存本次改动；若难以区分则 `git add` 合理范围后于提交信息中概括）  
3. `git commit -m "<描述>"`：根据实际改动写**完整、可读**的提交说明；**用户未写提交信息时由助理自拟，不得因缺省说明而停下来问用户**  
4. `git push origin main`（当前分支若非 `main`，推送到当前跟踪分支）  
5. 推送成功后执行 `npm run deploy:gh`（VitePress 构建并发布到 `gh-pages`，与本项目习惯一致）  

推送或部署失败时：检查网络与错误信息，可重试；仍失败则**明确告知原因**，**不要**为此先问「要不要重试」——应直接重试合理次数后再汇报。

## 关键词触发（补充）

用户消息若显式包含「提交、commit、push、部署、deploy、发布」等，同样执行上述完整流程（仍**不**要求用户确认）。

## 禁止项

- **禁止**使用「检测到需要提交的更改，是否立即执行提交+推送+部署？」类**执行前确认**。  
- **禁止**在无特殊原因时，以「未提供提交信息」为由暂停；由助理根据改动自拟 message 即可。

## 注意事项

- 长时间命令使用足够 `block_until_ms`（如 ≥60000）等待 `npm run deploy:gh` 完成。  
- 部署入口**固定**为 `npm run deploy:gh`，不另问其他部署方式。  
- 部署成功后可在回复中给出站点根地址（如 `https://yupli.github.io/Personal/`）及本次相关页面路径。
