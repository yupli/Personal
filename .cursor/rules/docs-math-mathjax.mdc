---
description: 站点文档中 LaTeX/数学公式的定界与 MathJax 规范（VitePress + markdown-it-mathjax3）
globs: "docs/**/*.md"
alwaysApply: false
---

# 数学公式与 LaTeX（MathJax 渲染）

本知识库站点：`vitepress` + `markdown-it-mathjax3`（见 `docs/.vitepress/config.mts`）。新增或编辑 `docs/**/*.md` 中公式时**必须**遵守以下定界与写法，否则会出现「方括号/括号里一坨未渲染 LaTeX」或行内空白等问题。

## 1. 定界符：只用 `$` 与 `$$`

| 类型 | 源码写法 | 说明 |
|------|----------|------|
| 行内 | `$...$` | 同一段落内嵌公式。 |
| 块级 | 单独一行的 `$$`，公式内容可多行，再以单独一行的 `$$` 结束 | 不要与行内定界符混在一行里写整块推公式（除非整段很短且确实只一行）。 |

**禁止**在 Markdown 源码里用 **`\(` `\)`**、**`\[` `\]`** 作为 TeX 定界符。

**原因（必读）**：CommonMark 会把 `\` 后若干标点当作**转义**处理。单写 `\[` 时，反斜杠被消耗，输出到 HTML 里往往只剩字面 `[`，MathJax 插件**收不到** `\[...]`，页面上就显示成带方括号的纯文本。`\(`、`\)` 同理，行内数学可能整段失效。

**参考实现**：`docs/or-opt/exact/lagrangian.md` 等已采用 `$` / `$$`，可作范本。

## 2. 块级公式 `$$` 的排版习惯

- `$$` **独占一行**；开头 `$$` 前、结尾 `$$` 后各保留空行或与正文用空行分隔，避免与列表/标题粘连解析错误。
- 多行对齐可用 `aligned` 等环境，写在同一对 `$$` 内（与现有 `lagrangian` 等页一致）。

## 3. 行内 `$` 的注意点

- 美元符号在公式外若作货币等用途，用反斜杠转义成 `\$`，避免被当成公式起点。
- 行内若出现 `_` 控制下标，确保整段在**同一对** `$...$` 内，且配对完整；否则可改用块级 `$$` 或 `\mathrm{}` 等避免与 Markdown 强调冲突（少见，优先保证 `$` 成对）。

## 4. 算法框 `::: algorithm` 内

- 与正文相同：行内用 `$...$`，独立公式用成对的 `$$`。
- 若某行只想写符号、不愿依赖 LaTeX，可用 Unicode 或直写（如 `Δ`、T₀）作为补充，以规则 `docs-style.mdc` 中算法与可读性要求为准。

## 5. 中文与 `\text{}`

- 在 `$...$` / `$$...$$` 内用 `\text{中文...}` 或 `\mathrm{...}` 等按 LaTeX 常规范即可；**不要**在 MathJax 定界符外把 `_`、`\` 等当作公式语法裸写，除非本意是普通文本并已转义。

## 5.1 勿在 HTML 块里写 `$` 数学（`figcaption` 等）

- VitePress 使用 `markdown-it` 时，**嵌在原生 HTML 里的文字**（如 `<figure>` / `<figcaption>` / `<div>` 内）通常**不会再**走一遍数学插件，结果会出现**字面量**的 `$`、`\_` 或 `\to`，与「未渲染」相同。  
- **处理**：图下说明若放在 `figcaption` 中，请用**Unicode**写下标与箭头（如 x₀、a₀、→、∈），或把含公式的说明移到 `</figure>` **之后**的**纯 Markdown 段落**（单独成行、不用 HTML 包裹），以便 `$...$` 被正常处理。

## 6. 式号：按「单文件」独立编号（勿在正文中写规则）

- **带 `\tag{·}` 的显式式号**仅在**当前 `.md` 文件内**自洽：从 $\tag{1}$、$\tag{2}$ 起顺排，**不**在站点内跨页面续号。  
- **禁止**在文档正文里用成段 **注** 或开篇段落重复说明「本页从 (1) 起编号」「与别页独立」等——上述约定**只**以本规则文件为据，由编辑/助理在写作时执行，**不要**写给读者看。  
- 若需与**外部**长文、印刷本或你站其他页**对读**，仅在确有必要时用一句 **注** 或行内补「某处式 (2.3)」等，**不**用 **注** 重复通用编号政策。  
- 图、表题仍可用带章节号的体例「图 2.12」「表 2.1」，与式号 (1) 起排互不冲突。

## 7. 自检

编辑或审阅时可在仓库根目录执行 `npm run build`；若公式仍不显示，先检查是否仍存在**单独成行的** `\[`、`\]` 或行内 `\(`、`\)`，全部改为 `$` / `$$` 后重试。检查 `\tag` 在同一 `.md` 内是否唯一、顺序是否合理。

## 8. 与其他文档规则的关系

- 全库 Markdown 的加粗、标题、附图等见 **`docs-style.mdc`**。  
- 本文件**仅**约束数学/ LaTeX 定界、渲染与式号习惯；二者同时适用时，以不冲突为则（定界以本文件为准）。
