---
description: 运筹 docs/or-opt 下启发式与元启发式文档的结构体例（开篇、主循环、子过程）
globs: docs/or-opt/**/*.md
alwaysApply: false
---

# 启发式 / 元启发式文档体例（`docs/or-opt/`）

本文件为**写作与协作规范**，供助理与作者维护笔记时遵循；**不要**把本文件中的条文整段复制进 `docs/**/*.md`。用户可见页面只写知识内容；体例以本规则为准。

## 适用范围

- 元启发式各算法子页：如 `heuristics/meta/simulated-annealing.md`、`heuristics/meta/tabu-search.md` 等（带主循环框的同类新篇亦同，均在 `docs/or-opt/heuristics/meta/`）。
- 总览页：`heuristic-algorithms.md`（启发式三类分类）、`metaheuristics.md`（元启发式篇目表）仅作分类、导航与一句话说明，**不**嵌入完整体例条文。

## 元启发式算法子页

- **开篇**：仅保留**一段**连续总述（背景、属于哪类方法、链到 `docs/or-opt/heuristics/metaheuristics.md`）。**不要**在文首设置「**适用**：」「**核心**：」等独立标签段（要点放入后文各节）。
- **正文顺序**：先写记号、机制、邻域/算子/参数、接受与调参要点等；**最后**用 `::: algorithm ... :::` 收束**对外的整体主循环**（与全站 `docs-style.mdc` 中 algorithm 容器规则一致）。
- **内层子过程**：若存在嵌套流程（如 VNS 中的 VND），子过程可先用独立段落或小 `algorithm` 收束；**整篇对外的「主」`algorithm` 块须排在子过程说明之后**，读者先懂子模块再对照最外层循环。
- **主循环之前**：用常规小节写清调参、实现注意、与精确子问题/子 MIP 的衔接等，勿依赖文首「适用/核心」代替。

## 总览页分工

- **`heuristics/heuristic-algorithms.md`**：三类划分、表与链接、折叠说明；可写知识性「边界」句，**不**写「写作体例」长段。
- **`heuristics/metaheuristics.md`**：一段定位 + 篇目表；**不**复述上条子页结构规范（链到 `heuristic-algorithms` 即可）。

## 单算法子页的叙事范式（教材感、易理解、不跑题）

- 目的：`docs/or-opt/` 下以讲清「当前条目的算法」为主（2-opt、插入、C-W 节约、元启发式子页等；总览/索引可略宽）。
- 结构：先定义「一步/邻域在做什么」→ 需要时用 SVG（页内可渲染）或 Mermaid 备稿讲结构 → 最后用 `::: algorithm` 只写本算法主循环；algorithm 不代替定义与图。
- 不跑题：避免在同页长篇展开 ILS/SA/PSO 等外层框架，需要时用一句话链到对应子页与 `heuristics.md` 类总览。
- 用户可见 Markdown 的加粗仍见 `docs-style.mdc`；本条为协作约定，不复制进 `docs/**/*.md` 正文。

## 正文可读性

- 同一段里若并列**多种**策略、情形、或「甲或乙或丙」式选项，用**无序列表**分条写清；少用分号、破折号把多件事挤成一句。  
- 需要对比步骤或顺序时，可用有序列表。公式仍单独成行；列表项内可含短公式。  
- 避免在单段里同时塞入「定义 + 三种做法 + 与另一概念的关系」而完全不换行、不分点。

## 与其他规则的关系

- 全文加粗、`###` 后换行、`algorithm` 容器语法等见 `docs-style.mdc`。
- 新增或调整 `docs/or-opt/` 下页面时，同步侧栏与索引见 `nav-sync.mdc`。
